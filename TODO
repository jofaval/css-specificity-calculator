Implement a user interface using HTML
Evaluate !important sentences in the CSS rules
Being able to compare and evaluate multiple CSS rules at once so that we can determine which one is the one with the highest specificity